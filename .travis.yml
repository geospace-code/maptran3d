# gfortran >= 5
language: c
group: travis_latest
dist: xenial

git:
  depth: 25
  quiet: true

env: FC=gfortran

addons:
  apt:
    packages:
    - gfortran

matrix:
  include:
  - os: linux
  - os: windows
    install: 
    - cd build
    - cmake -G "MinGW Makefiles" -DCMAKE_SH="CMAKE_SH-NOTFOUND" ..
    - cmake --build .

install: cd bin

script:
- cmake -Drealbits=32 ..
- cmake --build .
- ctest --output-on-failure

- cmake -Drealbits=64 ..
- cmake --build .
- ctest --output-on-failure


